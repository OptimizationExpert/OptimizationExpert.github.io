name: Daily SEO Opportunity Report

on:
  schedule:
    - cron: "10 9 * * *"
      timezone: "Europe/Dublin"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: daily-seo-opportunity-report
  cancel-in-progress: false

jobs:
  run-seo-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run SEO opportunity bot and email report
        env:
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_APP_PASSWORD: ${{ secrets.SMTP_APP_PASSWORD }}
          EMAIL_TO: optimizationteamonline@gmail.com
        run: |
          python seo_opportunity_bot_v4.py --output-base "$GITHUB_WORKSPACE" --no-open

      - name: Upload generated reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: seo-opportunity-report-${{ github.run_id }}
          path: seo_opportunity_output/
          if-no-files-found: warn
          retention-days: 30
