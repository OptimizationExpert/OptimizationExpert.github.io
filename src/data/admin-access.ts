/**
 * رمز عبور پنل ابزارهای مدیریتی سایت (/admin/).
 *
 * برای تغییر رمز، فقط همین یک خط را ویرایش کنید و سایت را دوباره build/
 * دیپلوی کنید (روی GitHub Pages یعنی فقط commit و push کنید؛ هش رمز به‌طور
 * خودکار در زمان build ساخته می‌شود، نیازی به اجرای هیچ اسکریپتی نیست).
 *
 * ⚠️ توجه: چون این سایت کاملاً استاتیک است و روی GitHub Pages میزبانی
 * می‌شود (بدون سرور)، این گیت یک لایه‌ی بازدارنده‌ی سبک در مرورگر است، نه
 * قفل امنیتی واقعی — کسی که HTML صفحه را ببیند هش رمز را هم می‌بیند و در
 * تئوری می‌تواند آفلاین آن را حدس بزند. برای اکثر کاربردها (جلوگیری از
 * دسترسی تصادفی بازدیدکنندگان عادی) همین کافی است.
 */
export const ADMIN_PASSWORD = 'optexpert-2026';

